// If...else Statement
const age = 22;
let access;

if (age > 21) {
  access = 'Yes';
} else {
  access = 'No';
}

console.log(access); // 'Yes'

// Equivalent Ternary Operator
const ageTernary = 22;
let accessTernary = ageTernary > 21 ? 'Yes' : 'No';

console.log(accessTernary); // 'Yes'
